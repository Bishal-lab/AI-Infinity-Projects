# Sends the BFSI & life insurance brief to Gmail every morning.
#
# The schedule is UTC, always. 01:30 UTC is 07:00 IST — an hour before the brief
# is wanted, and that hour is deliberate.
#
# GitHub's scheduler is best-effort and queues; it does not fire on the minute.
# The two scheduled runs so far both started ~60 minutes late:
#
#   asked 02:30 UTC → started 03:29:14 UTC (23 Aug)   59m late
#   asked 02:30 UTC → started 03:30:49 UTC (24 Aug)   61m late
#
# At 02:30 the brief could only ever arrive at or after 08:00 IST, and in
# practice arrived at 09:00. Asking an hour early puts 08:00 inside the window
# rather than at its floor: on a typical morning it lands at 08:00, and on a
# morning GitHub is quick, at 07:00 — early being the better failure.
#
# Re-measure before changing this. If the delay shifts, move the cron, not the
# expectation.
#
# Two more things worth knowing about scheduled workflows:
#   * GitHub disables them after 60 days with no commits to the default branch.
#     A single commit re-arms them.
#   * A scheduled run is best-effort. If GitHub drops one, the next morning's
#     26-hour window picks up the stories that were missed.
#
# The brief goes to e-mail only; Telegram is off in config/settings.yaml.
#
# Required repository secrets (Settings → Secrets and variables → Actions):
#   GMAIL_ADDRESS  GMAIL_APP_PASSWORD
# Optional: EMAIL_TO (defaults to GMAIL_ADDRESS), EMAIL_FROM_NAME
#
# The two TELEGRAM_* secrets below are read but unused while that channel is
# off. They stay wired up so turning Telegram back on is a one-line config
# change rather than a workflow edit as well.

name: BFSI digest

on:
  schedule:
    - cron: "30 1 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: What to do
        type: choice
        default: run
        options:
          - run             # build and send
          - dry-run         # build, upload the output, send nothing
          - preview         # build and print it into this job log, send nothing
          - check-sources   # fetch every feed and report on its health
          - test-delivery   # verify the Telegram and Gmail credentials
          - telegram-chats  # print the chat ids the bot can see

concurrency:
  group: bfsi-digest
  cancel-in-progress: false

permissions:
  contents: read

defaults:
  run:
    working-directory: bfsi-insurance-digest-bot

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      EMAIL_TO: ${{ secrets.EMAIL_TO }}
      EMAIL_FROM_NAME: ${{ secrets.EMAIL_FROM_NAME }}
      MODE: ${{ inputs.mode || 'run' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: bfsi-insurance-digest-bot/requirements.txt

      - name: Install dependencies
        run: pip install --disable-pip-version-check -r requirements.txt

      # The seen-store is what stops the overlapping window repeating yesterday's
      # headlines. The runner is thrown away after each job, so it is carried
      # between runs in the Actions cache: a fresh key every run, restored from
      # whatever the most recent run left behind.
      - name: Restore the seen-store
        uses: actions/cache/restore@v4
        with:
          path: bfsi-insurance-digest-bot/state
          key: bfsi-digest-state-${{ github.run_id }}
          restore-keys: bfsi-digest-state-

      - name: Send the digest
        if: env.MODE == 'run'
        run: python -m bot run

      - name: Build the digest without sending
        if: env.MODE == 'dry-run'
        run: python -m bot run --dry-run

      - name: Print the digest into the log
        if: env.MODE == 'preview'
        # Reads the brief without downloading an artifact, and without touching
        # the seen-store — so running it never costs you a story tomorrow.
        run: python -m bot preview

      - name: Check the feeds
        if: env.MODE == 'check-sources'
        run: python -m bot check-sources

      - name: Verify the delivery credentials
        if: env.MODE == 'test-delivery'
        run: python -m bot test-delivery

      # Setup only. A bot cannot message you first, so TELEGRAM_CHAT_ID does not
      # exist until you message the bot — and then only Telegram knows it. This
      # asks, using the token alone, and prints the ids here. The runner has the
      # open network the lookup needs; a laptop behind a proxy often does not.
      - name: Find the Telegram chat id
        if: env.MODE == 'telegram-chats'
        run: python -m bot telegram-chats

      - name: Save the seen-store
        # Runs even when delivery failed, so a retry does not re-send what did
        # get through. Skipped for the read-only modes, which never write it.
        if: always() && (env.MODE == 'run')
        uses: actions/cache/save@v4
        with:
          path: bfsi-insurance-digest-bot/state
          key: bfsi-digest-state-${{ github.run_id }}

      - name: Upload the rendered brief
        if: env.MODE == 'dry-run'
        uses: actions/upload-artifact@v4
        with:
          name: digest-preview
          path: bfsi-insurance-digest-bot/out
          retention-days: 7
